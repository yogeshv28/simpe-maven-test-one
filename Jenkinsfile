pipeline {
    agent any

    stages {
        stage('Maven-App Unit Tests') {
            steps {
                echo "Unit Test Execution"
            } 
        }
        stage('Maven-App Package') {
            steps {
                echo "crete jar file"
            } 
        }
    }
}
